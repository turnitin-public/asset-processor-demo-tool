version: "3"
agent:
    connect_url: connect.us.ngrok-agent.com:443
endpoints:
    - name: ap-demo
      url: https://lti-ap-demo.ngrok.io
      upstream:
        url: 9002
      traffic_policy:
        on_http_request:
          - actions:
              - type: remove-headers
                config:
                  headers:
                    - expect
